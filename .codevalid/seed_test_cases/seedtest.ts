/// <reference types="jest" />
/**
 * CodeValid seed test — validates that the Jest test environment is set up correctly.
 * DO NOT remove this file; it is used during onboarding validation.
 */
describe('CodeValid Seed Test', () => {
  it('CODEVALID_SEED_ASSERTION_OK', () => {
    // Emits the onboarding marker in Jest output for log validation.
    console.log('CODEVALID_SEED_ASSERTION_OK');
    expect(true).toBe(true);
  });
});
