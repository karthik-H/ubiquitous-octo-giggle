# Example environment configuration for Task Management API

# Database URL (for production, replace with actual DB connection string)
DB_URL=sqlite:///tasks.db

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO